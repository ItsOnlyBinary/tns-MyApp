const observableModule = require("tns-core-modules/data/observable");

function PlanetSelectViewModel() {
    const viewModel = observableModule.fromObject({});

    return viewModel;
}

module.exports = PlanetSelectViewModel;
